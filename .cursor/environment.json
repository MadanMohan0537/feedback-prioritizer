{
  "name": "Feedback Analyzer",
  "install": "sudo apt-get update && sudo apt-get install -y python3.12-venv && python3 -m venv .venv && . .venv/bin/activate && pip install --upgrade pip && pip install -r requirements-minimal.txt",
  "terminals": [
    {
      "name": "streamlit",
      "command": ". .venv/bin/activate && streamlit run app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true"
    }
  ],
  "ports": [8501]
}
