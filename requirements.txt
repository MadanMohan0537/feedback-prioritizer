# Minimal set to run the dashboard end-to-end with fallback models:
streamlit>=1.30
streamlit-autorefresh>=1.0
pandas>=2.0
numpy>=1.24
plotly>=5.18
scikit-learn>=1.3
vaderSentiment>=3.3
wordcloud>=1.9
matplotlib>=3.7

# Optional, for the full BERTopic + transformer-sentiment pipeline
# (auto-detected; app falls back gracefully if these aren't installed):
torch>=2.0
transformers>=4.35
sentence-transformers>=2.2
bertopic>=0.16
umap-learn>=0.5
hdbscan>=0.8
